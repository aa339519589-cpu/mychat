# MyChat Deployment Guide

This filename is retained only for old links. It is not an executable production procedure.

The sole authoritative release procedure is [MyChat control-plane deployment runbook](docs/deployment-runbook.md). It includes the current migration chain, maintenance boundary, PostgreSQL autocommit requirements, billing reconciliation gate, revision-aware readiness, rollback policy, and production evidence checklist.

Do not use the historical `0400/0500` bridge sequence for the current control plane.
