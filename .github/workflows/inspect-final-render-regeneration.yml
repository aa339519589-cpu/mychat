name: Inspect final Render regeneration state

on:
  pull_request:
    paths:
      - .github/workflows/inspect-final-render-regeneration.yml

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
    steps:
      - name: Capture final deployment and readiness
        run: |
          set -euo pipefail
          {
            echo '=== deploys ==='
            curl --fail-with-body --silent --show-error \
              --header "Authorization: Bearer $RENDER_API_KEY" \
              "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys?limit=8" \
              | jq '[.[] | {id:.deploy.id,status:.deploy.status,commit:.deploy.commit.id,createdAt:.deploy.createdAt,updatedAt:.deploy.updatedAt,finishedAt:.deploy.finishedAt}]'
            echo '=== ready ==='
            curl --fail-with-body --silent --show-error --connect-timeout 10 --max-time 30 \
              --header 'Accept: application/json' \
              https://mychat-nm6x.onrender.com/api/ready
            echo
          } | tee "$RUNNER_TEMP/final-render-regeneration.txt"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: final-render-regeneration-state
          path: ${{ runner.temp }}/final-render-regeneration.txt
          retention-days: 1
