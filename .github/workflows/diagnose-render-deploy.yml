name: Diagnose Render deploy

on:
  push:
    branches:
      - ops/deploy-ios-chat-standard-fetch

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
      RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
    steps:
      - name: Capture live Render state
        run: |
          {
            echo '=== deploys ==='
            curl --fail-with-body --silent --show-error \
              --header "Authorization: Bearer $RENDER_API_KEY" \
              "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys?limit=5" \
              | jq '[.[] | {id:.deploy.id,status:.deploy.status,commit:.deploy.commit.id,createdAt:.deploy.createdAt,updatedAt:.deploy.updatedAt,finishedAt:.deploy.finishedAt}]'
            echo '=== ready ==='
            curl --silent --show-error --connect-timeout 10 --max-time 30 \
              https://mychat-nm6x.onrender.com/api/ready
            echo
          } | tee "$RUNNER_TEMP/render-status.txt"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: render-status
          path: ${{ runner.temp }}/render-status.txt
          retention-days: 1
